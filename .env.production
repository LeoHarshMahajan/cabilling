# Copy this to .env on your server and fill in real values

NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_MIN_32_CHARS

# SQLite DB path — keep it outside the app folder so deploys don't wipe it
DATABASE_FILE=/var/www/ca-billing-data/prod.db
